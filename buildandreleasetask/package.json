{
  "name": "typescript-action",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript template action",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc -b",
    "format": "prettier --write '**/*.ts'",
    "format-check": "prettier --check '**/*.ts'",
    "clean": "rm -rf lib dist",
    "lint": "eslint packages/**/*.ts",
    "package-main": "ncc build lib/main/main.js --source-map --license licenses.txt -o dist/main",
    "package-post": "ncc build lib/post/main.js --source-map --license licenses.txt -o dist/post",
    "package": "npm run 'package-main' && npm run 'package-post'",
    "all": "npm run clean && npm run format && npm run lint && npm run build && npm run package"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/actions/typescript-action.git"
  },
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "azure-pipelines-task-lib": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.39",
    "@types/q": "^1.5.5",
    "@typescript-eslint/parser": "^5.8.1",
    "@vercel/ncc": "^0.31.1",
    "eslint": "^7.32.0",
    "eslint-plugin-github": "^4.3.2",
    "eslint-plugin-jest": "^25.3.2",
    "js-yaml": "^4.1.0",
    "prettier": "2.5.1",
    "typescript": "^4.4.4"
  }
}
